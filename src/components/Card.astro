<style>
  .card {
    background: var(--cardBgColor);
    margin: 0 auto;
    font-size: 1.2rem;
    padding: 16px;
    border-radius: 8px;
    color: var(--cardTextColor);
  }
</style>

<div class="card">
  <slot />
</div>
