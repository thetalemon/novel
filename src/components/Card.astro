<div class="card">
  <slot />
</div>

<style>
  .card {
    background: var(--cardBgColor);
    margin: 0 auto;
    font-size: 1.2rem;
    padding: 16px;
    border-radius: 8px;
    color: var(--cardTextColor);
    width: calc(100% - 16px * 2);
  }
</style>
