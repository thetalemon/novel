---
import { Image } from 'astro:assets'
import ReturnImage from '@/icons/home.gif'

export interface Props {
  type: 'return' | 'profile'
}

const item = { src: ReturnImage, alt: 'トップに戻る' }
---

<style lang="scss">
  img {
    width: 42px;
    height: 42px;
  }
</style>

<Image src={item.src} alt={item.alt} />
